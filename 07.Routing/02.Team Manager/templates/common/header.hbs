<header>
    <div class="jumbotron">
        <h1>Team Manager</h1>
        {{#if loggedIn}}
            <span>Welcome, {{email}}</span>
        {{/if}}
        <nav class="mt-3">
            <a href="#/home" class="btn btn-default btn-outline-secondary m-2">Home</a>
            <a href="#/about" class="btn btn-default btn-outline-secondary m-2">About</a>
            {{#if loggedIn}}
                <a href="#/catalog" class="btn btn-default btn-outline-secondary m-2">Catalog</a>
                <a href="#/logout" class="btn btn-default btn-outline-secondary m-2">Logout</a>
             {{else}}
                 <a href="#/login" class="btn btn-default btn-outline-secondary m-2">Login</a>
                 <a href="#/register" class="btn btn-default btn-outline-secondary m-2">Register</a>
            {{/if}}
        </nav>
    </div>
</header>